{
  "name": "vib_sns",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf docs && mkdir -p docs && cp -r src/* docs/",
    "start": "npx serve docs"
  },
  "devDependencies": {},
  "dependencies": {}
}
